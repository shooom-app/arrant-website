# Example environment variables (non-secret placeholders)
# Public site URL used for absolute OG/Twitter metadata (dev defaults to http://localhost:3000)
NEXT_PUBLIC_SITE_URL=

# Optional: in-memory rate limit (requests per IP per minute)
RATE_LIMIT=10

# Optional: future CRM/email integration (do not commit real secrets)
CRM_API_ENDPOINT=https://example-crm.com/api
CRM_API_KEY=your-key-here
CONTACT_EMAIL=info@example.com